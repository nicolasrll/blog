{% extends "back/template.html.twig" %}
{% block title%}Modifier un projet - {{ commonTitle }}{% endblock %}
{% block content %}
    <section class="container">
        <header class="text-center">
            <h1>Modifier un projet</h1>
            {% if flashMessage is not empty %}
                <p class="{{ classValue }}">{{ flashMessage }}</p>
            {% endif %}
        </header>
        {{ include ('back/_project_form.html.twig', {'action' : 'edit?id=' ~ projectId, 'tokenName' : 'tokenEditProject'  }) }}
    </section>
{% endblock content %}
